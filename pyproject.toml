[project]
name = "cerl-website"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pelican[markdown]>=4.10",
    "pelican-sitemap>=1.1",
    "typogrify>=2.0",
]

[dependency-groups]
dev = [
    "livereload>=2.6",
]
